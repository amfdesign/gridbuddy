{
  "name": "GridBuddy",
  "version": "0.1.0",
  "slug": "gridbuddy",
  "description": "Modulares Batterie- und Geräte-Management als HA Add-on",
  "startup": "services",
  "boot": "auto",
  "ingress": true,
  "host_network": false,
  "options": {
    "batteryCapacityWh": 10000,
    "chargePowerW": 3000,
    "dischargePowerW": 3000,
    "tibberToken": "",
    "haToken": "",
    "ollamaServer": ""
  },
  "schema": {
    "batteryCapacityWh": { "type": "number", "minimum": 0 },
    "chargePowerW":     { "type": "number", "minimum": 0 },
    "dischargePowerW":  { "type": "number", "minimum": 0 },
    "tibberToken":      { "type": "string" },
    "haToken":          { "type": "string" },
    "ollamaServer":     { "type": "string" }
  }
}
